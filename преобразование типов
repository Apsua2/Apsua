#include <iostream>

int main() {
    int number = 129956;
    int sum = 0;

    while (number != 0) {
        sum += number % 10;
        number /= 10;
    }

    std::cout << "Сумма цифр числа 129956: " << sum << std::endl;

    return 0;
}




// 2) Чтобы доказать, что треугольник со сторонами 34, 17 и 98 может существовать, нужно проверить выполнение условия треугольника. В треугольнике сумма длин двух сторон всегда должна быть больше длины третьей стороны.

// Давайте проверим это для сторон 34, 17 и 98:
// - 34 + 17 > 98 - условие выполняется
// - 34 + 98 > 17 - условие выполняется
// - 17 + 98 > 34 - условие выполняется

// Таким образом, условие существования треугольника выполняется для сторон 34, 17 и 98. Такой треугольник может существовать.
